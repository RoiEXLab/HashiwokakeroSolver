language: cpp
sudo: required
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7
before_install: sudo ln -sf /usr/bin/g++-7 /usr/bin/g++ && g++ --version
script: ./gradlew -Dorg.gradle.console=plain check
