language: cpp
sudo: required
matrix:
  include:
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-7
before_install:
  - export 'CXX=g++-7 -Wall -std=c++11'
script: ./gradlew -Dorg.gradle.console=plain check
